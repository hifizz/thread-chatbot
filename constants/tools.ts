// Tool names shared between the chat API route (server) and the tool UI
// registrations (client), so both sides always refer to the same tool.
export const MARKDOWN_DOCUMENT_TOOL_NAME = "createMarkdownDocument"
