## 1. 标题模型与输出策略

- [x] 1.1 在方舟常量中声明 Coding Plan 的 `doubao-seed-2.0-mini` 标题模型及输出 token 安全阀。
- [x] 1.2 将 `POST /api/branch-title` 切换为方舟轻量模型，禁用自动重试并移除字符/英文单词截断。

## 2. 自动调用去重

- [x] 2.1 在分支领域状态和 store 中记录标题生成尝试，并让候选筛选跳过已尝试分支。
- [x] 2.2 在当前标签页记录尝试状态并传入树标识，覆盖防抖持久化完成前的刷新窗口。

## 3. 展示与验证

- [x] 3.1 保持完整标题存储，确认各前端标题容器仅按可用宽度使用 CSS 省略展示。
- [x] 3.2 运行 `pnpm typecheck` 与 `pnpm lint`，确认无类型或 lint 错误。
