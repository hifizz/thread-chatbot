## 1. 标题生成

- [x] 1.1 使用统一标题请求支持 MainThread 与 ForkedThread 输入，复用火山方舟小模型、清洗和单次调用策略。
- [x] 1.2 按现有树结构建立一次性标题候选；保持各自输入上下文与失败回退。
- [x] 1.3 保持自动标题、树机器标题和用户自定义标题的既有优先级。

## 2. 规范与验证

- [x] 2.1 将标题能力规范更名为 `title-generation`，并与 domain spec 术语一致；不改变 Thread 的持久化模型。
- [x] 2.2 运行类型检查、lint、OpenSpec 严格校验和差异检查。
