## 来源
Site appgprj_6aa2fd9ba0648191a8cee43d8e761654，第 3 版，源码 29c0d1d8c8ffd4de475f9d4259667c2f3b68be38。

## 接入
根路由保留服务端 metadata，交互首页作为客户端组件。剧本、播放状态、分栏、游标和弹层分别封装。复用 Base UI Tabs/Accordion；演示弹层使用 Base UI 的受控 Root、显式锚点和 Portal，挂载在首页演示容器中。所有样式限定在 threadchat-landing 中，演示不使用真实工作台的 tc 类，避免路由切换后的 CSS 干扰。

## 产品边界
演示使用预设回答，不调用模型、不创建真实会话。开始使用沿用 ROUTES.startChat；不提供假报名成功。保留隐私政策、服务条款。首页无 GitHub 链接；仓库仍为 public，闭源的仓库设置与许可证决定需另行处理。

## 验证
类型检查、改动范围 ESLint、生产构建和可行的浏览器交互检查。重点验证六套剧本切换、分支弹层、引用、暂停/重播、窄屏横向溢出和入口链接。
