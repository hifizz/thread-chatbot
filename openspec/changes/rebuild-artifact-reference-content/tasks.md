## 1. T1：统一内容协议与操作入口

- [x] 1.1 核对实现时 main 与 #93 的差异，列出 send/edit/fork/retry、草稿恢复及模型组装入口；标明可移植代码和需替换代码，保留已有修复。
- [x] 1.2 在现有 contracts 中扩展唯一内容 Schema 及推导类型，定义引用输入、持久快照和完整有序草稿；落实保序规范化与未知版本错误。
- [x] 1.3 统一草稿导出、持久消息恢复及纯派生函数，移除调用处自行按类型重拼内容的逻辑；验证交错 text/file/quote/reference、重复引用和空格往返保持。
- [x] 1.4 将内部 send/edit 改为单一 content，fork 改为显式 firstTurn，retry 读取已保存消息；保留 HTTP v1 parts 和现有幂等 ID，不保留平行 text/files 输入。
- [x] 1.5 通过真实命令边界验证分叉首问、空分叉、非法 firstTurn、编辑顺序及 Quote 删除/来源校验；确认旧 text/file/quote 消息兼容。每批源码改动运行仓库要求的 typecheck。

## 2. T2：集中引用解析与实体约束

- [x] 2.1 在现有 Repository 增加 Project 范围批量加载，单一应用解析入口在已有事务中验证所有权、来源状态和权威快照；仅允许新的 completed Markdown 引用。
- [x] 2.2 将引用 Part 接入持久化和读取链路，打通文字加一个 Markdown 引用的最小纵向流程；支持 schemaVersion:1，禁止把客户端标题或正文作为权威数据。
- [x] 2.3 核查全部 Artifact 写入路径，保证同 ID 内容与来源不可变、更新生成新 ID；历史回放与编辑保留旧引用不受来源替代/归档影响。
- [x] 2.4 用真实数据库测试未知、跨用户、跨 Project、未完成及伪造引用均在模型调用前失败且无半条消息；同时覆盖当前 Thread 引用、旧引用保留与新 Project 拒绝旧引用。

## 3. T3：归位模型上下文编译与预算

- [x] 3.1 将 #93 可复用的引用展开和去重逻辑归位到单一应用模块；按实际上下文前向扫描，使用固定标记，不修改持久化 Parts 或前文。
- [x] 3.2 验证源工具匹配、完整/临时/部分正文、重复提及、冻结 forkContext、历史裁剪及 SDK 最终消息；仅最终保留的完整正文可计入 seen，并比较相同配置下前缀字节稳定性。
- [x] 3.3 落实最多 20 次提及和单消息唯一 Artifact 标题/正文合计 200,000 JavaScript 字符的接收限制；验证重复引用保序且正文预算只计一次。
- [x] 3.4 核实已有模型限制和各类输入计量能力，在最终请求边界统一计算 system、工具、历史、附件/多模态、引用与输出预留；已知超限提前拒绝，无法可靠估算时显式标记 unknown，并统一处理提供商超限错误。
- [x] 3.5 验证聚合后超限、边界值及计量未知场景；记录估算方式和限制，不以字符数代替 token，不把正文写入额外诊断日志。

## 4. T4：按 Lexical 官方方式拆分输入框

- [ ] 4.1 核对项目 Lexical 版本及直接依赖，先以官方公开 Typeahead 能力验证底部翻转、窄列、画布变换和软键盘；遇到能力缺口提交最小复现、版本和取舍并向用户求助，不增加内部定位补丁。
- [x] 4.2 按官方 Node/Plugin/Command 机制拆分 MessageComposer、引用节点、候选菜单、提交和草稿同步职责，建立唯一 codec；禁止新增编辑器适配框架或平行内容状态。
- [x] 4.3 明确 Lexical 活动文档与 Store 完整草稿的同步边界，保留文件 localId/顺序及上传状态；仅显式加载/切换/重置导入，迟到发送结果不得覆盖后续输入或其他 Thread。
- [ ] 4.4 实现 @ 和按钮选择、标题/类型/来源与中文检索、键盘选择和 IME；用官方公开节点与剪贴板接口验证原子删除、撤销重做、复制粘贴，普通 @ 不自动成为引用。
- [x] 4.5 从 normalized Store 资源切片订阅候选，关闭时不查询、无关流式更新不重建；历史引用导航通过独立回调接入，并移除静默替代 Store 及旧内部 DOM/CSS 定位代码。
- [x] 4.6 验证列/画布切换、编辑恢复、Quote/File/重复引用交错、附件上传及提交后的继续输入，确保最终提交顺序与完整内容一致。

## 5. T5：真实验收与交付

- [ ] 5.1 在可用真实数据库及模型环境验收跨 Thread 和当前 Thread Markdown 引用的生成、选择、发送、读取、刷新恢复、编辑、分叉与重试；记录环境、SHA 和结果，替身测试不作为真实 E2E 完成证据。
- [ ] 5.2 浏览器验收中文输入法、键盘、原子删除/撤销/粘贴，以及底部、窄列、画布、软键盘菜单布局；官方能力阻塞项保持未完成并向用户报告。
- [x] 5.3 回归旧 text/file/quote 消息、权限拒绝、历史不可变性、上下文去重和整体预算；按仓库要求运行相关测试与 typecheck，不执行额外格式化或在 feature 分支生成迁移。
- [x] 5.4 对照两轮 CR 映射和三份规格逐项核验，删除已替换的多源输入及定位补丁；检查没有遗留双协议或无调用实现。
- [x] 5.5 更新任务状态与验证证据，说明实际阻塞、兼容性和回滚方式；核对 main 新改动后交付实现 PR，不关闭或合并 #93，未获真实证据的项目不勾选。
