# 2026-08-07

## 首页（落地页）改版：全中文化 + 视觉重排

按用户要求重排 `app/page.tsx` 落地页，落地 openspec change `redesign-landing-page` 的体验意图：

- `constants/landing.ts`：全部文案改为中文；新增 `statement.primaryCta`（立刻开始）与 `statement.githubCta`（GitHub 外链）；新增 `closing` 文案块；`navCta`/`primaryCta` label 统一为「立刻开始」；footer links 改为 GitHub/隐私政策/服务条款。
- `components/landing/product-statement.tsx`：在主标题（h1）下新增两按钮行——绿色实心「立刻开始」+ 黑色描边「GitHub」（外链 target=_blank）。GitHub 图标用内联 SVG（lucide-react 此版本已移除 `Github` 品牌图标）。
- `components/landing/closing-cta.tsx`：改为读取 `LANDING.closing` 文案，不再硬编码英文。
- `components/landing/landing.module.css`：editorial/closing h2 改 `text-align:center; white-space:nowrap`（单行居中，移动端字号收窄到 clamp(1.28rem,6.2vw,1.7rem) 防溢出）；正文 `.prose/.introduction` 显式 `text-align:left`；`.closing` 整块居中、按钮全宽；新增 `.ctaRow`（居中 flex 换行）与 `.ctaLinkSecondary`（描边次级按钮样式）。
- `app/page.tsx` metadata 标题/描述改中文。
- `pnpm typecheck` 通过。

设计要点：标题居中单行、正文左对齐、底部 CTA 居中；卖点体现在「从那句话开始 / 主线始终清爽 / 沉淀为知识」三条差异。

## 排版微调（第二轮）

根据截图反馈进一步收口视觉：

- `landing.module.css`：`.videoFrame` 收窄到 `width: min(100%, 720px)` + `max-height: clamp(340px, 42vw, 440px)`（不再 800 全宽占满首屏）；`.hero` 加 `padding-bottom: clamp(24px, 3vw, 36px)`。
- 占位分支 `.fallbackBranch` 改为水平三分布（`top:50%; transform:translateY(-50%)`，中间再 translateX(-50%)），高度从 18% 提到 46% 让卡片更挺。
- `.statement` 容器从 560 加宽到 680，避免主标题「在任意一句话上，岔开一条新思路」在桌面端被 `text-wrap: balance` 强制断成两行；移除 h1 的 `text-wrap: balance`，字号上限从 3.12rem 降到 2.45rem，line-height 1.08，保证单行排布。
- 描述段 `max-width` 从 520 提到 560，margin-top 调到 20px，line-height 1.7，与标题留白更协调。
- 移动端：`.statement` 回到 560，`h1` 字号 clamp(1.42rem, 7vw, 1.85rem) 保证 12 字内不溢出；视频框 max-height 320。
- `pnpm typecheck` 通过。

## 重构：文案内联，删除 constants/landing.ts

用户反馈「首页字符串不可复用的别放 constants，太复杂」。把 `constants/landing.ts` 里的 `LANDING`/`LandingContent` 全部拆解，文案就近内联到各 landing 组件：

- 删除 `constants/landing.ts`（整个文件）。
- `components/landing/types.ts`：本地定义 `LandingCta`（label/href/accessibleLabel?），不再从 constants 引。
- `hero.tsx`：`VIDEO_SRC`/`VIDEO_DESCRIPTION`/`SLOGAN` 顶部常量。
- `product-statement.tsx`：`TITLE`/`DESCRIPTION`/`PRIMARY_CTA`/`GITHUB_CTA`，GitHub 链接取 `PROJECT.repositoryUrl`，立刻开始取 `ROUTES.startChat`。
- `founder-story.tsx`：`TITLE` + `PARAGRAPHS`。
- `why-not-existing-tools.tsx`：`TITLE`/`INTRODUCTION`/`DIFFERENCES`。
- `closing-cta.tsx`：`TITLE`/`DESCRIPTION`/`PRIMARY_CTA`。
- `landing-header.tsx`：`NAV_CTA`，aria-label 改「首页」。
- `landing-footer.tsx`：`FOOTER_LINE` + `FOOTER_LINKS`（显式 `FooterLink` 接口，避免 `as const` 导致 `external` 联合类型缺字段）。
- 保留在 constants 的仅剩可复用项：`ROUTES`（路由）、`PROJECT`（仓库名/链接/版权年）。
- `pnpm typecheck` 通过；landing 目录无 lint 报错。

## 进一步：去掉组件顶部 const 变量，文案直接写进 JSX

用户再反馈「不要把文案提成组件里的 const 变量，直接写死到 JSX 里」。重写所有 landing 组件：

- 删掉 `hero/product-statement/founder-story/why-not-existing-tools/closing-cta/landing-header/landing-footer` 顶部的 `TITLE/DESCRIPTION/PARAGRAPHS/DIFFERENCES/FOOTER_LINKS/NAV_CTA` 等模块级常量。
- 文案直接写在 JSX 文本节点里（`<h1>在任意一句话上，岔开一条新思路</h1>`、`<p>...</p>`、三条 `<li>` 直接展开不再 `.map`）。
- `LandingCtaLink` 保留为可复用展示组件，调用处传内联对象 `{ label: "立刻开始", href: ROUTES.startChat }`。
- `landing-footer.tsx` 三条链接直接写三个 `<li>`，不再用数组，`FooterLink` 接口随之删除。
- `types.ts` 的 `LandingCta` 接口保留（供 `LandingCtaLink` 的 props 类型用）。
- `pnpm typecheck` 通过。
