# Frontend / Styling

- Wants CSS refactored away from raw files joined by `@import` cascade order into a managed, tooling-based architecture (Sass/Less, or a Tailwind-plugin-style abstraction like `@tailwindcss/typography`). Confidence: 0.85
- Strongly prefers semantic design tokens: every fontsize, padding, margin, and background should carry meaning (e.g. `--surface-raised`, `--text-body`) rather than hardcoded pixel values. Confidence: 0.9
- Wants responsive typography/readability controlled via CSS variables + classNames across breakpoints (mobile / pc / lg screens), instead of scattered per-file media queries. Confidence: 0.85
- Wants tokens and colors managed through a config layer that supports override and extension (theme config / Tailwind theme tokens). Confidence: 0.85
- When defining conventions (token taxonomies, naming grammars, patterns), wants them grounded in and traceable to established industry design systems (e.g. Material 3, IBM Carbon, GitHub Primer, Atlassian, Apple HIG, Polaris, Ant Design) rather than invented ad hoc — asked to extend the semantic categories "from big-company styleguides you know". Confidence: 0.8
- Inviolable rule (user-declared "不可打破的原则"): px values must be integers, preferably even — fractional px like `font-size: 12.5px` are rejected and rounded to nearest even (12.5→12, 13.5→14); values that genuinely can't be even (sub-pixel `letter-spacing: 0.5px`, `1.5px` borders, negative hot-zone margins) stay but must carry an inline flag for later re-inspection (e.g. `/* tc-review: 非偶数值，保留原因 */`). Confidence: 0.95
- In styling refactors, tokenize ALL colors first as the initial round — including collapsing every `color-mix()` into a derived variable/token — before migrating other value categories (spacing, sizing, etc.). Confidence: 0.9
- Color organization must be deliberately re-planned by proximity: similar colors grouped adjacent (e.g. paper family → ink family → depth family → functional colors → whites/shadows) so palette files read as clusters. Confidence: 0.9
- z-index must always be controlled through semantic variables (named layer roles, e.g. column-local layers vs overlay layers with stacking-context audit notes), never bare numeric literals. Confidence: 0.95
- Cares that finished styling modules (e.g. the `.tc-prose` typography module) are reusable in other Tailwind projects — asked about cross-project portability before confirming: values self-contained, zero-runtime, purely CSS-variable-driven forms (vendor copy → npm package → Tailwind `@plugin`) over build-tool-coupled ones (Sass mixins would become dead code once copied out). Confidence: 0.65
- Styling-refactor acceptance runs on two tracks that must never be mixed: value-preserving steps (token renames, same-value re-pointing like `#b07d2e` → `--tc-depth-2`, deleting dead fallbacks) must prove computed-style equivalence (grep zero-hit lists + build-output comparison), while any value-altering fix (e.g. re-targeting the unsaved-state gold to depth-1) is logged as a registered visual change and moved to the screenshot-baseline track — ambiguous historical intent gets logged as a decision, never silently fixed. Confidence: 0.7
- For fallible user-input flows such as attachment uploads, prefers graceful recovery over pretending errors can be eliminated: show failures explicitly, preserve the user's input, and let them remove or retry it. Confidence: 0.9
- Expects attachment inputs to support common plaintext and source-code extensions (including Markdown and JavaScript) rather than limiting acceptance to files the browser labels `text/plain`. Confidence: 0.9
- In chat message bubbles, wants image previews width-constrained so they do not stretch the bubble, and wants non-image files represented as visible attachment cards rather than bare links or omitted UI. Confidence: 0.9
- Wants user-facing UI copy stripped of development-process text, implementation details, and engineering jargon; labels, states, and errors should be phrased around what ordinary users need to understand and do. Confidence: 0.95
- For pasted or voice-transcribed text, prefers a character-count-based hybrid UX: shorter content should be inserted directly into the composer rather than uploaded and shown as an attachment, while genuinely long content can remain an attachment. Confidence: 0.9
- For fast-demo UI controls, prefers reusing the project's existing shadcn components with no custom styling or visual-polish work; place controls directly beside the related existing control (for example, generation menus immediately to the right of the model selector). Confidence: 0.95
- Expects mention/reference chips to behave as frozen atomic content: the caret must stay outside, left/right navigation must cross the chip, clicking then typing must preserve it, and clickable references should open the corresponding preview. Confidence: 0.95
- Expects trigger popovers to be fully keyboard accessible: visibly highlight the initial item, support Up/Down and Enter, keep an informative empty panel when no resources match, and let Escape close it without deleting the typed trigger. Confidence: 0.95
- Prefers compact content-sized popovers with minimal dead space; a one-item or empty state should be about one row tall plus padding, with empty-state copy centered horizontally and vertically. Confidence: 0.95
- During model wait periods, wants the chat UI to render the agent trace: chain-of-thought auto-expanded while streaming (shimmer header) interleaved with tool-call rows and web-search source rows ("边思考、边调用工具、边反思"); when done it collapses to a one-line summary (e.g. "思考了 N 秒" / "已搜索网络 · N 个来源") that stays manually re-expandable; history messages render in the already-collapsed done state. Confidence: 0.9
- When a vendored third-party primitive lacks a production capability (e.g. controlled run state), extend it minimally with additive optional props mapped onto its existing internal derivation so the demo path behaves unchanged; all business/data mapping lives in the host-side adapter component. Confidence: 0.85
- When vendoring a third-party CSS foundation into an existing design system: scope its tokens under a wrapper class (dark via `.dark .wrapper`), split page-shell rules (canvas pattern, typography) into a separate demo-only class so production mounts only the token layer, inline plugin-provided values (layered shadow stacks) instead of adding the global plugin, keep token values untouched and document mechanical adjustments in a file-header comment, and grep the host for utility-class name collisions before integrating. Confidence: 0.85
- Chat message content blocks (thinking traces, inline panels) must hug their content: auto height with a max-height cap, shrinking below the cap; a fixed min-height that leaves blank space under short content is treated as a bug ("读取网页高度不对，空白大片"). Confidence: 0.9
- Inline chat components must derive width from the surrounding message prose container (w-full, following `.md-body` / `.tc-prose`, which is `max-width:100%`) — never carry the vendored demo's fixed width (e.g. ThinkingState's official `max-w-95` / 380px) into the production conversation UI. Confidence: 0.9
- While the thinking trace streams, its content region is capped at a max-height and auto-scrolls bottom-pinned so the newest text stays visible ("内容要不断往上滚动，因为它是流式输出"). Confidence: 0.9
- Capped scrollable content regions should show HeroUI-ScrollShadow-style scroll hints: a top/bottom fade (mask gradient) enabled only when hidden content exists in that direction, toggled from scroll position. Confidence: 0.9
- Prefers warm cream / buttery-yellow (奶油黄) palette over cool gray/blue-gray — finds too-gray UI mismatched with body text and wants components (e.g. thinking trace) remapped onto the warm paper/ink token system so the whole conversation reads as a cohesive warm tone. Confidence: 0.85
- Wants demo/placeholder/example content removed as product matures toward user perspective — generic demo strings like "best waffle cone supplier" should not appear in user-facing panels such as "已读取网页"; production UI should show only real, user-relevant content. Confidence: 0.9
- Expects scrollable thinking/content regions to be scrollbar-free and shadow-free when content does not overflow the max-height cap (e.g. only ~3 lines) — scrollbars and ScrollShadow fades are affordances only when there is actually hidden overflowed content above/below. Confidence: 0.9
- Expects a persistent streaming progress affordance: the three-dot typing animation should stay visible while streaming until visible text starts (even when thinking/tool traces are already shown), then swap to a caret once text streams. Confidence: 0.85
- Wants completed/revealed reasoning text rendered with `white-space: pre-wrap` so authored line breaks and meaningful whitespace are preserved for readability. Confidence: 0.95
- For a completed reasoning trace containing only one sentence, prefers a lightweight single line of muted text instead of full collapsible chrome (no “已思考” heading, disclosure arrow, or vertical guide); reserve the collapsible treatment for streaming or longer, multi-part reasoning, following mature-chatbot information hierarchy. Confidence: 0.9
- For high-volume Deep Research runs, prefers a dedicated panel that contains and organizes the complex process (plans, reasoning, requests, tool calls, reflection, replanning, and repeated calls) rather than flooding the main chat timeline with hundreds of webpage events; keep only one compact progress/completion entry in chat, and group the panel’s full history by research round/batch instead of flat-listing every page visit. Confidence: 0.95
- Expects research timelines to label each operation by its actual kind: `webSearch` should appear as searching and `readUrl` as reading, rather than deriving every row’s label from the message-level route and mislabeling reads as searches. Confidence: 0.85
- Wants webpage-read (`read`) trace rows represented with the `BookOpen` icon. Confidence: 0.95
